

export type RecoveryStoreType = {
  email: string
  setEmail: (email: string) => void
  isSuccess: boolean
  setIsSuccess: (isSuccessful: boolean) => void
  isLoading: boolean
  setIsLoading: (isLoading: boolean) => void
}

